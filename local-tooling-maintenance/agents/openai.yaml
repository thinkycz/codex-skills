interface:
  display_name: "Local Tooling Maintenance"
  short_description: "local tooling maintenance workflows"
  default_prompt: "Use $local-tooling-maintenance for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
