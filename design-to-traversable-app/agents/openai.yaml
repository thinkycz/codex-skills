interface:
  display_name: "Design To Traversable App"
  short_description: "design to traversable app workflows"
  default_prompt: "Use $design-to-traversable-app for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
