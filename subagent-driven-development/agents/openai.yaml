interface:
  display_name: "Subagent Driven Development"
  short_description: "Execute through lead-worker slices with clear approval boundaries"
  default_prompt: "Use $subagent-driven-development to keep planning and integration with the lead, brief bounded workers with canonical repo examples and exact gates, then run independent gap review and final verification where risk warrants it."

policy:
  allow_implicit_invocation: true
