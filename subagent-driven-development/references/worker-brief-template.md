# Worker Brief Template

```md
## Worker Slice

- Behavioral goal:
- Acceptance condition:
- Owned files or subsystem:
- Do not change:
- Prerequisite state and dependency contract:
- Downstream consumer or integration point:
- Canonical example files to follow:
- Non-obvious repo conventions:
- Generated-file policy:
- Canonical generator or scaffold command, if any:
- Known nearby edits or collaboration constraints:
- Required targeted checks:
- Full-project integration gate:
- Review mode: implementation worker | independent read-only gap reviewer
- Expected final report:
```

Validate generator names, namespaces, and output locations against the canonical examples before downstream work depends on them.
