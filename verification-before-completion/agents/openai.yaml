interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh, race-safe lifecycle evidence"
  default_prompt: "Use $verification-before-completion to choose the cheapest sufficient evidence layers, verify the exact installed or deployed path, use the active host's canonical visible browser when runtime evidence matters, and report missing evidence precisely."

policy:
  allow_implicit_invocation: true
