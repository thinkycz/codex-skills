interface:
  display_name: "Verification Before Completion"
  short_description: "verification before completion workflows"
  default_prompt: "Use $verification-before-completion to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
