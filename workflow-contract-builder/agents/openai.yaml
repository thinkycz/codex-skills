interface:
  display_name: "Workflow Contract Builder"
  short_description: "workflow contract builder workflows"
  default_prompt: "Use $workflow-contract-builder for the requested work within its stated boundary."
