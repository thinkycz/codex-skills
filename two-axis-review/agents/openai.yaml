interface:
  display_name: "Two-Axis Review"
  short_description: "two axis review workflows"
  default_prompt: "Use $two-axis-review for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
