interface:
  display_name: "Native Desktop Delivery"
  short_description: "native desktop delivery workflows"
  default_prompt: "Use $native-desktop-delivery for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
