interface:
  display_name: "Requirements Review"
  short_description: "requirements review workflows"
  default_prompt: "Use $requirements-review to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
