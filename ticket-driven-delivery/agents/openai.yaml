interface:
  display_name: "Ticket Driven Delivery"
  short_description: "ticket driven delivery workflows"
  default_prompt: "Use $ticket-driven-delivery to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
