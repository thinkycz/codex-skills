interface:
  display_name: "Repo Convention Discovery"
  short_description: "Inspect repo patterns, gates, and safe starting points"
  default_prompt: "Use $repo-convention-discovery to inspect this repository, identify the full-project validation gates and non-obvious enforced conventions, summarize the patterns we should follow, and identify likely entrypoints and safe starting places."
