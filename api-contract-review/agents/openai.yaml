interface:
  display_name: "Api Contract Review"
  short_description: "api contract review workflows"
  default_prompt: "Use $api-contract-review for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
