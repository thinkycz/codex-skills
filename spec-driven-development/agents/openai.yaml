interface:
  display_name: "Spec-Driven Development"
  short_description: "Deliver proportionally from client specs"
  default_prompt: "Use $spec-driven-development to verify a tracked docs home, deliver this source through a proportional tier and one clear owner, reuse shared UI and email primitives, and verify the result against the source through the active host's canonical browser when runtime evidence matters."
