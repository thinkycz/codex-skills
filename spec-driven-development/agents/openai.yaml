interface:
  display_name: "Spec-Driven Development"
  short_description: "Deliver proportionally from client specs"
  default_prompt: "Use $spec-driven-development to implement this client specification with a proportional small, medium, or large delivery tier, one clear owner, traceable progress where it adds value, and verification against the source."
