# Validate

Run package and owner/mode/dependency validation, regression tests and read-only generated-artifact checks. Generate ignored artifacts only with explicit generation commands. Check semantic contents, not just matching fingerprints or counts. Preserve previous evaluation records as historical; changed sources make them stale. For independent testing, hide expected owners and rubrics, retain exact responses and report unrun/blocked cases honestly.
