interface:
  display_name: "Skill Maintenance And Validation"
  short_description: "skill maintenance and validation workflows"
  default_prompt: "Use $skill-maintenance-and-validation to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
