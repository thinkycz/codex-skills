interface:
  display_name: "Session Handoff"
  short_description: "session handoff workflows"
  default_prompt: "Use $session-handoff to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
