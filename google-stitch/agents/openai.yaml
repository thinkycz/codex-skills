interface:
  display_name: "Google Stitch"
  short_description: "google stitch workflows"
  default_prompt: "Use $google-stitch for the requested work within its stated boundary."

policy:
  allow_implicit_invocation: true
