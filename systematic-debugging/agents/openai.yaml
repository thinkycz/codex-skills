interface:
  display_name: "Systematic Debugging"
  short_description: "Root-cause debugging with proportional evidence records"
  default_prompt: "Use $systematic-debugging to prove root cause from the exact running artifact, compare API and worker process facts side by side, trace queues and provider handoffs through the final outcome, and verify the smallest justified fix."
