interface:
  display_name: "Full Project Hardening"
  short_description: "Audit a product and verify authorized fixes"
  default_prompt: "Use $full-project-hardening to inventory this product, audit the full stack, respect audit-only scope or implement authorized fixes in bounded slices, run an independent gap review, and verify the integrated result before a release verdict."

policy:
  allow_implicit_invocation: true
