interface:
  display_name: "Full Project Hardening"
  short_description: "full project hardening workflows"
  default_prompt: "Use $full-project-hardening to handle the requested outcome in the appropriate mode."

policy:
  allow_implicit_invocation: true
