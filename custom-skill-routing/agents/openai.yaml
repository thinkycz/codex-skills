interface:
  display_name: "Custom Skill Routing"
  short_description: "custom skill routing workflows"
  default_prompt: "Use $custom-skill-routing to handle the requested outcome in the appropriate mode."
