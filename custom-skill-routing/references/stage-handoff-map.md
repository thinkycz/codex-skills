# Stage handoff map

Use current evidence and the requested outcome, not missing document names, to choose the next owner. Sufficient inline requirements can proceed directly to authorized delivery. Existing implementation normally needs only the incomplete slice, verification or requested review. Consult the route matrix only when ownership is unclear. Reuse the working summary and invalidate facts only when relevant inputs change.
